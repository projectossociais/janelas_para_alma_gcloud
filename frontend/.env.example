# Copiar para .env e preencher. Nunca comitar o .env real — ver .gitignore.

# URL da API própria (FastAPI). Em dev local sem Docker: http://localhost:8000
# Dentro do docker-compose: já injectado em build-time (ver docker-compose.yml).
VITE_API_URL=http://localhost:8000
